export * from '../../../../scripts/core/fileConstants.mjs';

/**
 * Name of the `web` docker image.
 */
export const webDockerImageName = 'chorro-web';

/**
 * User name of the person or organization who owns the `web` docker image.
 */
export const webDockerImageOwner = 'skeswa';
