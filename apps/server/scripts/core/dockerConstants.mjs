export * from '../../../../scripts/core/fileConstants.mjs';

/**
 * Name of the `server` docker image.
 */
export const serverDockerImageName = 'chorro-server';

/**
 * User name of the person or organization who owns the `server` docker image.
 */
export const serverDockerImageOwner = 'skeswa';
