{
  "name": "@chorro/server",
  "private": true,
  "version": "0.0.2",
  "description": "Backend for chorro.app",
  "license": "MIT",
  "scripts": {
    "build": "GOOS=linux GOARCH=amd64 go build -o ./build/server-linux-arm64",
    "deploy": "node ./scripts/deploy.mjs",
    "dev": "dotenv nodemon main.go",
    "format": "go fmt",
    "lint": "go vet",
    "release": "node ./scripts/release.mjs",
    "test": "go test"
  },
  "devDependencies": {
    "dotenv-cli": "^4.1.1",
    "nodemon": "^2.0.15"
  }
}
