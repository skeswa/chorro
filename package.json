{
  "name": "chorro",
  "private": true,
  "scripts": {
    "test": "turbo run test"
  },
  "devDependencies": {
    "@changesets/cli": "^2.19.0",
    "prettier": "^2.5.1",
    "turbo": "^1.0.23"
  },
  "turbo": {
    "baseBranch": "origin/main",
    "pipeline": {
      "build": {
        "dependsOn": [
          "lint",
          "^build"
        ]
      },
      "deploy": {
        "dependsOn": [
          "build",
          "test",
          "lint"
        ]
      },
      "dev": {
        "cache": false
      },
      "lint": {
        "outputs": []
      },
      "format": {},
      "test": {
        "dependsOn": [
          "build"
        ]
      }
    }
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
